- configure the cloudflare s2 service/bucket? this is for mainly the saved audio/video etc for the 24hr windows.

- proxy integration
Webshare is fine — just click Static Residential,
  confirm it says unlimited bandwidth and a per-IP price
  (not per-GB), and you've got exactly the flat-rate pool we
  designed the code for.

  https://www.webshare.io/static-residential-proxy?source=nav_bar

- google plugin which has the entire feature set. paid only (subscription?). this would be the most profitable i think since it would probably be based on the users ip entirely

- how does the our website impose the limits? just by ip? can we get it to be more robhust, eg device id? since they could just vpn and do that.

- probably need to include "ⓘ" on all the feature pages for whatever limitations of thetools. such as highest quality to download is based on the video uploaded itself. eg if the video only has 1080p uploaded, we cannot offer 4k download.

- do we offer change email ability for paid users? this would be easier if we ujst gave a license key but idk that has its own issues, id rather not go the license key route.

- choose domain + branding kit
available DNS:
YTPuller.com
ytvideo.tools (good redirect)
YTtooling.com
